model_name: bert-base-multilingual-cased

batch_size: 16
learning_rate: 0.00002
num_epochs: 3
temperature: 0.07
projection_dim: 128
max_length: 128

languages:
  - en
  - tr
max_samples_per_language: 500

checkpoint_output_path: outputs/checkpoints/supcon_mbert.pt
